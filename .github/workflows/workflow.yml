---
name: 'Deploy2'

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: korniyana/qwerty/.github/actions/hello@master
      with:
        name: OctoCat
